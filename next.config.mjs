/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['placeholder.url', 'hebbkx1anhila5yf.public.blob.vercel-storage.com', 's4.anilist.co', 'cdn.myanimelist.net', 'cdn.noitatnemucod.net', 'static1.cbrimages.com'],
  },

  eslint: {
    ignoreDuringBuilds: true,
  },
};

export default nextConfig;
